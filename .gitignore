node_modules/
dist/
out/
*.zip
# T22: built CLI bundle + packed tarball are reproducible from `pnpm build:cli`
# / `pnpm pack`. The bin lives at bin/polar-to-strava-fit.mjs, the tarball at
# polar-to-strava-fit-*.tgz. Both are regenerated at publish time by the
# `prepublishOnly` hook.
bin/
*.tgz
# T15: the demo-data sample ZIP is the one ZIP we DO commit. Vite serves it
# from public/ at /<base>/sample-polar-export.zip for the "Try with sample
# data" CTA. Generated by `pnpm build:sample`.
!public/sample-polar-export.zip
.DS_Store
.vite/
*.log
.env
.env.local
coverage/
playwright-report/
test-results/
.claude/
